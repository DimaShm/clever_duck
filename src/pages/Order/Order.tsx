import './Order.scss';

export const Order = () => {
  window.scrollTo({
    top: 0, left: 0, behavior: 'smooth',
  })

  return (
    <div className="Order">
      <h1>Order Page</h1>
    </div>
  );
};
